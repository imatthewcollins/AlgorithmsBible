//
//  BinarySearch.swift
//  AlgorithmsBible
//
//  Created by Collins, Matthew - MC on 23/11/2023.
//

import Foundation

func binarySearch(data: [Int], target: Int) -> Bool {
    
    // 0. Is the length of data <= 1?
    // 0.1 If so, return if data == target
    // 1. Calculate midpoint index
    // 2. Is the target less than or greater than the midpoint index?
    // 3. If less than, call binarySearch with the left half of the array
    // 4. If greater than, call binarySearch with the right half of the array
    
    return true
}
