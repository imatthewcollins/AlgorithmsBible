//
//  MergeSort.swift
//  AlgorithmsBible
//
//  Created by Collins, Matthew - MC on 23/11/2023.
//

import Foundation

func mergeSort(data: [Int]) {
    
    // 0. if the length of array is <= 1, return array
    // 1. find the midpoint of the array
    // 2. have two new arrays to store each half of the array
    // 3. call mergeSort with the left array
    // 4. call mergeSort with the right array
    // 5. create a new array the same length as the original data array
    // 6. create a pointer for the current item being checked for both the left and right array
    // 7. iterate over length of new array
    // 8. compare the left and right pointer, adding the smallest value to the new array and incrementing that array's pointer
    
}
