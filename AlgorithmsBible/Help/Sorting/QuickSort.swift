//
//  QuickSort.swift
//  AlgorithmsBible
//
//  Created by Collins, Matthew - MC on 22/11/2023.
//

import Foundation


func quickSort(data: [Int]) -> [Int] {
    
    // 0. if data.count <= 1 { return data }
    // 1. Choose a pivot
    // 2. Compare each item in data to the pivot
    // 3. If less than pivot, put in left array
    // 4. If greater than pivot, put in right array
    // 5. leftSorted = quickSort(data: left)
    // 6. rightSorted = quickSort(data: right)
    // 7. Merge left + pivot + right
    
    return []
}
