//
//  BubbleSort.swift
//  AlgorithmsBible
//
//  Created by Collins, Matthew - MC on 23/11/2023.
//

import Foundation

func bubbleSort(data: Int) {
    
    // 1. Iterate over the length of array
    // 2. Iterate over the length of the array, starting at 1st index
    // 3. Compare the value at the current index with the value at the previous index
    // 4. If value at previous index is greater than value at current index, "swap" items by using a variable "temp"
    
}
